spring:
  redis:
    host: localhost
    database: 0
    password: zzzz
    lettuce:
      pool:
        max-active: 8
        min-idle: 0
        max-wait: 10s
        max-idle: 8
server:
  port: 8081
